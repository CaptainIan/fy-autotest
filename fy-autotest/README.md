#### 环境安装
```bas
$ pip3 install git+https://github.com/CaptainIan/fasttest.git -i https://mirrors.aliyun.com/pypi/simple
$ pip3 install -r requirements.txt
$ python3 -m playwright install chromium # 安装 chrome 浏览器内核
```
